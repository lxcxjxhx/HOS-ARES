[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "argus-languages"
version = "0.1.3"
description = "Built-in security scanner for Java, PHP, Flutter, Terraform, Ansible, Python, Go, and 20+ languages. No external tools. pip install && argus-languages scan ."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = [
    "argus", "security", "sast", "terraform", "ansible", "java", "php", "flutter", "dart",
    "devsecops", "code-scanning", "iac", "kotlin", "golang", "ruby", "docker", "kubernetes",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
]
dependencies = [
    "pyyaml>=6.0",
]

[project.scripts]
argus-languages = "argus_languages.cli:main"

[project.urls]
Homepage = "https://github.com/OkiriGabriel/argus-codescan-mcp"
Repository = "https://github.com/OkiriGabriel/argus-codescan-mcp"
Documentation = "https://github.com/OkiriGabriel/argus-codescan-mcp/tree/main/packages/languages"

[tool.hatch.build.targets.wheel]
packages = ["src/argus_languages"]

[tool.hatch.build.targets.sdist]
include = ["/src", "/README.md", "/LICENSE"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["argus_languages"]
