# round-14：compressReleaseAssets 对 assets/rootfs.tar.xz（~150-180MB）压缩时
# zipflinger 需大堆 → 2g 触发 "Java heap space"（round-12 实测日志），提至 4g。
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official